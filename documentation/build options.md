## Build options

while building you can build in two distinct ways:

```bash
    dotnet build -c ENet      # uses original Server.cs (ENet)
    dotnet build -c AltNet    # uses Server2.cs (Custom Solution)
```

